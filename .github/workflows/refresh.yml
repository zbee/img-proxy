name: Refresh cached images

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger worker refresh
        run: curl -sf "https://images.zbee.codes/refresh?key=${{ secrets.REFRESH_KEY }}"
